# RRFC — Rec Room Football Community

Static site for the league.

## Local preview

Open `index.html`, or run `npx --yes serve .` and open the printed URL.

## Media Editor

Open `media-editor.html` from your PC (`file://` or localhost). It is blocked on the live site.
